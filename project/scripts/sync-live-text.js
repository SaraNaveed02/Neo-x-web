/**
 * TEXT-ONLY sync reference from https://neoxweb.pages.dev/
 * Rule: update copy in existing HTML — do NOT change layout, CSS, or structure.
 *
 * Live URL map (no .html extension):
 *   /           → index.html
 *   /services   → services.html
 *   /about      → about.html
 *   /contact    → contact.html
 *   /team       → team.html
 *   /portfolio  → portfolio.html
 *   /web-development → web.html
 */
console.log('Text mappings are applied directly in HTML files.');
console.log('Re-fetch live pages and update scripts/sync-live-text.js when copy changes on neoxweb.pages.dev');
